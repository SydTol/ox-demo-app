const {SolidNodeClient} = require('solid-node-client');
const client = new SolidNodeClient();

(async()=>{
  let r = await client.fetch(`file://${process.cwd()}/foo.acl`);
  console.log(r.headers.get('Content-Type'));
})();
